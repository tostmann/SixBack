# robots.txt — sixback.io
# Permissive by design: general and AI crawlers are welcome to index this site.
User-agent: *
Allow: /

# Nothing useful to index in these — dynamic proxies, diagnostics, and the
# large firmware images (the web flasher and devices fetch those directly,
# not via crawlers, so this only spares crawlers needless bandwidth).
Disallow: /stations/
Disallow: /snapshots/
Disallow: /proxy/
Disallow: /*.bin$

# /max/ is the build for installations with an unusually large number of
# speakers. It is handed out deliberately, not browsed to: installing it
# requires a full erase, and it is pointless for everyone else. Kept out of
# the index so nobody lands there by search (the page also sends noindex).
Disallow: /max/

Sitemap: https://sixback.io/sitemap.xml
